/**
 * Next.js 14 no soporta next.config.ts (esa feature es de versiones más
 * nuevas) — se usa .mjs en su lugar. JSDoc para tipado en el editor sin
 * necesitar TypeScript en este archivo.
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
